<h1>Cocktails</h1>
<ul>
  <% @cocktails.each do |cocktail| %>
    <li><%= cocktail.name %> | <%= link_to 'Show details', cocktail_path(cocktail) %></li>
  <% end %>
</ul>

<%= link_to 'Create a new cocktail', cocktails_new_path %>
